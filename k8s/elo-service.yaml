apiVersion: apps/v1
kind: Deployment
metadata:
  name: elo-service
  namespace: hexchess
spec:
  replicas: 1
  selector:
    matchLabels:
      app: hexchess-elo
  template:
    metadata:
      labels:
        app: hexchess-elo
    spec:
      containers:
        - name: elo
          image: ghcr.io/k15z/hexchess-zero/training:latest
          args: ["elo-service", "--num-workers", "2"]
          envFrom:
            - secretRef:
                name: hexchess-env
          env:
            # Each game worker uses ~3 CPU for MCTS + ORT intra-op threading.
            # With num_workers=2 that's 6 CPU total, matching the pod request.
            - name: OMP_NUM_THREADS
              value: "3"
            - name: ORT_NUM_THREADS
              value: "3"
            - name: RAYON_NUM_THREADS
              value: "3"
          resources:
            requests:
              cpu: "6"
              memory: "16Gi"
            limits:
              cpu: "6"
              memory: "16Gi"
